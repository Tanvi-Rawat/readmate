import 'package:flutter/material.dart';

class UserLib extends StatelessWidget {
  const UserLib({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        "Library Page",
        style: TextStyle(fontSize: 40),

      ),
    );
  }
}